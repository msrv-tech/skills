interface:
  display_name: "Form Remove"
  short_description: "Удалить форму из объекта 1С (обработка, отчёт, справочник, документ и др.)"
  default_prompt: "Use $form-remove for this task."
policy:
  allow_implicit_invocation: true
