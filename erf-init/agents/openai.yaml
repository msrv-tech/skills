interface:
  display_name: "ERF Init"
  short_description: "Создать пустой внешний отчёт 1С (scaffold XML-исходников)"
  default_prompt: "Use $erf-init for this task."
policy:
  allow_implicit_invocation: true
