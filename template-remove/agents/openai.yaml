interface:
  display_name: "Template Remove"
  short_description: "Удалить макет из объекта 1С (обработка, отчёт, справочник, документ и др.)"
  default_prompt: "Use $template-remove for this task."
policy:
  allow_implicit_invocation: true
