interface:
  display_name: "EPF"
  short_description: "Используй этот скилл для всех операций с внешними обработками 1С (EPF): создание (init),..."
  default_prompt: "Use $epf for this task."
policy:
  allow_implicit_invocation: true
